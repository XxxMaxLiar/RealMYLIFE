1. **По метрикам в Kubernetes:** Мы смотрели на потребление ресурсов подом через `kubectl top pods` или в дашборде мониторинга (например, Grafana + Prometheus). Если потребление памяти (`memory`) постоянно упирается в лимит, указанный в `limits` пода, или потребление CPU близко к 100% от лимита, это был явный признак нехватки ресурсов.
    
2. **По поведению пода:** Kubernetes сам начинает убивать поды. Если не хватает памяти, контейнер получает сигнал `OOMKilled` (Out Of Memory). Если не хватает CPU, приложение не убивается, но начинает сильно тормозить, так как ядро ограничивает его процессорное время. В обоих случаях мы видели в статусе пода `CrashLoopBackOff` или бесконечные рестарты."
[[Если спросят что конкретно]]

####  А что вы делали, если видели `OOMKilled` или нехватку CPU?
    
    - _Ваш ответ должен быть таким:_ "Первым делом смотрели логи и метрики приложения, чтобы понять, это проблема кода (утечка памяти, неоптимальный алгоритм) или просто неправильно подобранные `limits` в конфигурации пода. Если с приложением всё okay, мы увеличивали `limits` в манифесте и перевыкатывали под. Если проблема в коде — писали тикет разработчикам с приложенными логами и метриками."

#### **Что вы делали, если сервер не выдерживал**
1. **Масштабирование:** Самый быстрый способ — через `kubectl scale` увеличить количество реплик проблемного сервиса, чтобы распределить нагрузку.
    
2. **Перераспределение:** Если проблема на конкретной ноде — `cordon` и `drain` чтобы убрать с нее нагрузку.
    
3. **Оптимизация:** После снятия пика分析ровали логи и метрики, чтобы найти коренную причину: неоптимальный код, медленные запросы или слабые `limits` у контейнеров. Дальше либо правили конфиги, либо ставили задачу разработчикам."
    

**Идеально завершить ответ так:**

"Конечно, идеальная ситуация — когда это делает автоскейлинг. Поэтому моей задачей было не только тушить пожары, но и настраивать HPA и правильно рассчитывать `requests/limits` для подов, чтобы таких ситуаций было как можно меньше."

#### **А как именно ты настраивал HPA? На какие метрики он ориентировался?"**
Чаще всего на среднюю утилизацию CPU по всем репликам деплоймента. Например, чтобы HPA добавлял реплики, если средняя загрузка CPU превышала 70%. Иногда на кастомные метрики из Prometheus, например, на длину очереди сообщений в RabbitMQ
        
#### **"А что делать, если в кластере вообще нет свободных ресурсов для новых подов?"**
    Тогда должен сработать Cluster Autoscaler, который автоматически добавит новую ноду в кластер. Если его нет, нужно было вручную увеличивать размер нод-группы в облачном провайдере (например, в GKE или EKS

#### А как нам понять что мы упёрлись в память или CPU?
**«Смотрим в два места:**

1. **Статус пода в Kubernetes:** Команда `kubectl describe pod...`. Если видим в событиях (Events) **`OOMKilled`** — это прямое указание, что контейнеру не хватило памяти и его прибили.
    
2. **Метрики потребления:** Команда `kubectl top pod` или дашборд в **Grafana**. Если видим, что потребление памяти или CPU постоянно **упирается в лимит (limit)**, указанный для контейнера, — это главный сигнал. Приложение живо, но работает на пределе и в любой момент может упасть.»