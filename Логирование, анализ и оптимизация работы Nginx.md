#### **Типы логов**

1. **Доступ (Access Log)**: фиксирует каждый запрос к серверу.  
    Файл по умолчанию: `/var/log/nginx/access.log`.
2. **Ошибки (Error Log)**: записывает ошибки, возникающие в работе сервера.  
    Файл по умолчанию: `/var/log/nginx/error.log`.